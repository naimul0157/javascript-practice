let name = 'naimul islam';
console.log(name.slice(2, 5));      //slice funtion used to slice the element of by index number
let part = name.split(", ");     //split function is used to convert the string into an array 
console.log(part);
let friends = 'Rahim, karim, dahim, lahim, sahim';
let array = friends.split(", ");
console.log(array);
console.log(array.join("-"));