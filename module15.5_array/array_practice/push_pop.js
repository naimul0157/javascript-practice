// push js to push value in array
let number = [12, 14, 15, 20, 30];
console.log(number);
number.push(40);
console.log(number);
// pop value from array to remove the last element inside an array
const out1 = number.pop();
const out2 = number.pop();
console.log(number);
console.log(out1, out2);